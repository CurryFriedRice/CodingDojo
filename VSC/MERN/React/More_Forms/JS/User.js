

/**
 * @summary A basic user for for Prop it up
 * @param {string} firstName;
 * @param {string} lastName;
 * @param {int} age;
 * @param {string} color;
 */
class User
{
    constructor(firstName, lastName, age, hairColor)
    {
        this.firstName = firstName;
        this.lastName = lastName;
        this.age = age;
        this.hairColor = hairColor;
    }
}



export default User;

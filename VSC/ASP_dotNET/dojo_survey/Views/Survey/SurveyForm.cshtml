<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link href="~/css/style.css" type="text/css" rel="stylesheet">
    <title>Document</title>
</head>
<body>
    <div class="container position-absolute top-50 start-50 translate-middle bg-primary p-3 text-light rounded shadow">

    <form class="d-flex flex-column" action="result" method="post">
        <div class="d-flex justify-content-between">
        <label for="name">Name</label>
        <input  type="text" name = "name" placeholder="Name" class="inputWidth rounded p-1">
        </div>
        
        <div class="d-flex justify-content-between mt-3">
        <label for="place">Meetup Place</label>
        <select name="place" class="inputWidth form-select">
            @{
                foreach(string place in ViewBag.Locations)
                {
                    <option type="text" value="@place">@place</option>
                }
            }
        </select>
        </div>
        
        
        <div class="d-flex justify-content-between mt-3">

        <label for="activity">What we doin?</label>
        <select name="activity" class="inputWidth form-select">
            @{
                foreach(string activity in ViewBag.Activities)
                {
                    <option type="text"  value="@activity">@activity</option>
                }
            }
        </select>
        </div>
        <label for="comment" class="mt-3">Anything else?</label>
        <textarea type="text"  name="comment" placeholder="Any concerns?" class="p-1 rounded"></textarea>
        <button class="btn btn-warning smaller  mt-3">Submit</button>
    </form>
    </div>
</body>
</html>